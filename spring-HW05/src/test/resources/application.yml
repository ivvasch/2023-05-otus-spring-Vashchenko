spring:
  shell:
    interactive:
      enabled: false
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:h2:mem:default
#    url: jdbc:postgresql://localhost:5430/postgres
#    username: postgres
#    password: postgres
  h2:
    console:
      enabled: true
server:
  port: 9092
